# CampaignLens Web Frontend Local Environment (apps/web/.env)
# Copy this file to apps/web/.env when running `pnpm --filter web dev` locally.

# API Base URL for TanStack Start & React Query
# Local backend: http://localhost:8787
# Production backend: https://campaign-lens-api.demostore.workers.dev
VITE_API_URL="http://localhost:8787"
